# Backend API URL
NEXT_PUBLIC_BACKEND_URL=http://localhost:3000

# Google Analytics Tracking ID
NEXT_PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# Eruda Debug Tool Configuration
# Options: auto (dev only), true (always), false (never)
ERUDA_ENABLED=auto